   <div class="header">
      <div class="container">
        <h1>Groups</h1>
      </div>
    </div>

    <div class="main">
      <div class="container">

        <ul class="comments">
          <% @groups.each do |p| %>
       <li> <%= link_to p.title, group_path(p) %> </li>
         <% end %>
        </ul>

      </div>
    </div>

